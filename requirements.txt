# regwatch 依赖清单
#
# 首选安装方式（uv，从 pyproject.toml 解析）：
#   uv sync --extra web --extra dev
#
# 或使用 pip 兼容安装：
#   pip install -e ".[web,dev]"
#
# 本文件仅作历史兼容参考，权威依赖声明见 pyproject.toml。

# 网页端
streamlit>=1.40
plotly>=5.24

# 模型 / CLI / 数据 / 采集
openai>=1.60
typer>=0.12
rich>=13.0
pandas>=2.2
requests>=2.32
beautifulsoup4>=4.12
aiohttp>=3.9
pypdf>=4.0
olefile>=0.47
python-docx>=1.1
tqdm>=4.66
